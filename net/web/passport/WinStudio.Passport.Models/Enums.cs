using System;

namespace WinStudio.Passport.Models
{
    public enum ContactType
    {
        Email,
        QQ,
        Phone,
        Bolg
    }

    public enum MStatus
    {
        Working = 0,
        Closed = 1
    }
}
